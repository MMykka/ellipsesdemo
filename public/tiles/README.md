Put your built regional `.pmtiles` file here, named `region.pmtiles` (or point `VITE_PMTILES_URL`
at a different name/path). See `docs/OFFLINE_SETUP.md` for how to build one. Not committed to the
repo — see `.gitignore`.
